#!/bin/sh

. dmenu_base

menu_file=~/.local/share/dmenu/menu.list
# Get user selection via dmenu from emoji file.
chosen=$(awk -F "\t" '{print $1}' $menu_file | dmenu $base $DB -p " ")

# Exit if none chosen.
[ -z "$chosen" ] && exit

to_run=$(grep "$chosen" $menu_file| sed "s/.*\t//g")
$to_run
