#!/bin/sh

. dmenu_base

chosen=$(find /usr/share/man/ -type f | awk -F '/' '{print $6}' | sed 's/...gz//g' | sort | dmenu $base $DB -p "man")

[ -z $chosen ] && exit

man -Tpdf $chosen | zathura - && exit
